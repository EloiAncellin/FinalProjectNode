version: '3'

services:
    web:
        build: ./
        ports:
            - "${WEB_PORT}:${WEB_PORT}"
        links:
            - "mongodb:${DB_HOST}"
        volumes:
            - "./src:/code/src"
            - "./.env:/code/.env"
        container_name: web
        environment:
            - DEV=0

    mongodb:
        image: "mongo:${DB_TAG}"
        container_name: mongodb
